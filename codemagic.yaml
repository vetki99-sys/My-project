workflows:
  android-workflow:
    name: Android Debug Build
    max_build_duration: 60
    scripts:
      - name: Build APK
        script: |
          chmod +x gradlew
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
